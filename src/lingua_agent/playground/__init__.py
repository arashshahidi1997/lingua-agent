"""Streamlit-based playground UI.

For iteration and demo, not production. The real product UI is the FastAPI +
React PWA planned in Phase 8 (see docs/clients.md). This module exists so
you can click around the existing pipeline today.

Launch:  lingua-agent playground
"""
